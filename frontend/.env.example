# The API now lives in this same Next.js app, so requests are same-origin and
# no base URL is needed. Set NEXT_PUBLIC_API_URL only if the API is ever split
# back out to a separate host.
# NEXT_PUBLIC_API_URL=
